import 'package:flutter/material.dart';

class callscree extends StatelessWidget {
  const callscree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'CALLS',
        style: TextStyle(fontSize: 20.0),
      ),
    );
  }
}
